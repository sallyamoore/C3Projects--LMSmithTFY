<div class="main-page">
  <h1>Let me <span class="smith"><span id="s">S</span><span id="m">m</span><span id="i">i</span><span id="t">t</span><span id="h">h</span></span> that for you</h1>

  <%= form_tag(search_path, :method => "get") do %>
    <div class="search-form">
      <%= text_field_tag :search, params[:query], class: "form-field", placeholder: "morbid and pale" %>
      <button class="btn-search" type="submit">Smiths Search</button>
    </div>
  <% end %>

  <!-- RAN OUT OF TIME FOR THIS BUT MIGHT COME BACK TO IT LATER -->
  <%= form_tag(feeling_miserable_path, :method => "get") do %>
      <button type="submit">I'm Feeling Miserable</button>
  <% end %>

</div>
